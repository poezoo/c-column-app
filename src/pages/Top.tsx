import React, { useState } from 'react';

export const Top: React.FC = () => {
  function handleClick(e) {
    e.preventDefault();
  }

  return (
    <section>
      <h2>top page</h2>
    </section>
  );
};
